# Docs Website Module

Opinionated wrapper for static documentation or marketing sites.

This composes the shared `static-website` module with a simpler default content
security policy suitable for brochure, docs, and release-note sites that do not
need the full application API/auth connectivity profile.
