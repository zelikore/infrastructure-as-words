# Example Website With API Module

Reference composition module that wraps the production `app-environment`
baseline with example-friendly tags and naming.

Use this when teams need a full example website baseline with API + history.
