# Cognito Web Client Module

Creates an environment-specific Cognito user pool client connected to the
shared user pool.

This module configures OAuth authorization code flow and session token
durations for SPA clients.
